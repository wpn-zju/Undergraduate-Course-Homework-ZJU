Fraction类
包含
三种构造函数
Fraction();
Fraction(int a, int b);
Fraction(const Fraction& that);
已重载的运算符
算术运算 + - * / += -= *= /=
逻辑运算 == != > >= < <=
输入流 >> 
输入格式为题目要求
输出流 <<
转字符串Fraction.ToString();
类型转换double

编译后运行main.exe可以查看测试程序运行情况
也可以修改测试程序进行测试